import React from 'react';
import './styles.scss';

const ProductCard = ({ children }) => {
    return (
        <>
        <header></header>
        {children}
        </>
    );
};

export default ProductCard;